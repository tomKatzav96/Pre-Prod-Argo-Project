# Values file

name: weather

replicaCount: 1

image:
  repository: tomkatzav/argo-weather:VERSION_NUMBER
  pullPolicy: IfNotPresent

service:
  name: weather-service
  type: NodePort  
  portName: my-port
  protocol: TCP
  port: 5000
  targetPort: 5000
  nodePort: 30100


